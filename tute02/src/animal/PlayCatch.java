package animal;


/*
    Contains only method stubs that classes which implement the interface
    must implement

    You cannot store data members* (you can static members) in an interface
*/
public interface PlayCatch {
    // * we can store static data members in interfaces if we like
    static final double pi = 3.14159;

    public void playCatch();
}
